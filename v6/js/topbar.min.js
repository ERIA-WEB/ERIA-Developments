function setFocusOnModal() { $("#searchModal").on("shown.bs.modal", function () { $("#searchBarInput").focus() }) } const onScroll = document.getElementById("desktopNav"); window.addEventListener("scroll", () => { window.scrollY < 500 ? onScroll.classList.remove("scrolled") : onScroll.classList.add("scrolled") }); let dropdowns = document.querySelectorAll(".c-navbar-top-dropdown"); dropdowns.forEach(e => { e.addEventListener("mouseenter", s => { e.classList.contains("closed") && e.classList.remove("closed") }), e.addEventListener("mouseleave", s => { e.classList.contains("closed") || e.classList.add("closed") }) }); const megaMenus = document.querySelectorAll(".mega-menu"); function clickmenumobileFunction() { $("#menu").css("transform", "translate(0%, 0)") } megaMenus.forEach(e => { e.addEventListener("mouseenter", s => { e.classList.add("showed") }), e.addEventListener("mouseleave", s => { e.classList.remove("showed") }) });